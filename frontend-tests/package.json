{
  "name": "playwright-demowebshop-template",
  "version": "1.0.0",
  "private": true,
  "description": "Automa\u00e7\u00e3o E2E com Playwright + TypeScript no Demo Web Shop, usando POM, fixtures e screenshots/trace on failure.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen http://localhost:5173/",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "prettier": "^3.3.3",
    "@types/node": "^20.11.30"
  }
}