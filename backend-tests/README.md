## ⚙️ Testes de Backend

Este projeto contém testes de integração e unitários da API.

### 🧪 Tipos de testes

- Testes unitários (regras de negócio)
- Testes de integração (requisições HTTP)

### 🚀 Como executar

```bash
cd Backend.Tests
dotnet test